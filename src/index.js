import React from 'react';
import ReactDOM from 'react-dom';
import Box from './Box';
import './index.css';

ReactDOM.render(
	let boxes = [['a'],['b'],['c']];
	<Box content={boxes} />,
	document.getElementById('root')
);
